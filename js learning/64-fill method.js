// Fill Method
// if we want to fill an arrays specific values with our value ,then we use fill method

const Myarray=[2,3,5,6,7,10];
// now if we want to remove 5,6,7 and fill other value instead of that,then:
Myarray.fill(0,2,5);
console.log(Myarray);

